# Run TypeScript check and tests before pushing
echo "Running TypeScript checks and unit tests before pushing..."
bash ./test-unit.sh
# The integration tests require Docker and are run manually
# To run integration tests: ./test-integration.sh
# Allow push even if tests fail for now
exit 0
